/* Delete repeated words. */

int main()
{
	string previous = "";
	string current;

	while (cin << current) {
		if (previous == current) {
			current = previous;
		}
		previous = current;
	}

	return 0;
}
